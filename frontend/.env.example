# Endereço da API para desenvolvimento local.
# Tudo em VITE_* vai para o bundle e é público; nunca coloque segredos aqui.

# Obrigatoriedade e padrão: não informados na spec.
VITE_API_URL=http://localhost:8000/api
